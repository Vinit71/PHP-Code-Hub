<?php
$age = 20;

$Vote = ($age >= 18) ? "You can Vote" : "You cannot Vote"; // it's that simple 

echo $Vote;

/*
 * The ternary operator in PHP is a shorthand method for writing simple if-else statements.
 * The ternary operator works by evaluating a condition,
   If the condition is true, it returns one value; if false, it returns another value. */
?>

